# `.design-system.md` — Editorial Burgundy / Paper

> 출처: `app/worldcup/wc-tokens.css` + `design-references/worldcup-design-2026-05-08/project/{prediction.html,styles.css}`. 월드컵 페이지 시안에서 추출. 사용자 승인 필요 — 이 문서는 Phase 1+ 작업의 single source of truth.
>
> **이미 격리되어 있음**: 모든 토큰은 `.worldcup-scope` 클래스 아래에서만 유효. 글로벌(`app/globals.css`)의 shadcn/Tailwind 토큰과 충돌하지 않음.

---

## 1. Aesthetic vibe (한 단락)

**에디토리얼 스포츠 매거진**: 따뜻한 크림색 페이퍼 배경 + 깊은 버건디 단일 강조 + uppercase letter-spaced "eyebrow" 레이블 + 무게감 있는 (font-weight 800–900) 큰 헤딩 + tabular-nums 숫자 + 카드 위쪽에 4px 컬러 보더로 그룹 정체성 표시. 그림자는 거의 안 쓰고 1px 라인으로 윤곽. Hero만 진한 그라디언트 + blurred orbs + 격자 마스크 + 회전된 황금 prize ribbon로 시각 임팩트. 차분함 + 화려함이 한 페이지 안에서 공존.

---

## 2. Color tokens (`--wc-*`, 격리됨)

### Core palette
| Token | Value | 역할 |
|---|---|---|
| `--wc-burgundy` | `#A0203B` | Primary 강조 (CTA bg, eyebrow 색, 강조 링크) |
| `--wc-burgundy-deep` | `#7E1A2F` | Hero gradient 시작점, hover 위에서 더 어두운 톤 |
| `--wc-ink` | `#1A1416` | 본문 / 헤딩 텍스트 (검정 대신) |
| `--wc-paper` | `#FAF7F4` | **페이지 배경** — 크림색. 흰색 절대 아님 |
| `--wc-card` | `#FFFFFF` | 카드 배경 |
| `--wc-line` | `#EFE7E0` | 1px 라인 / 카드 보더 |
| `--wc-line-2` | `#E5DAD2` | 강한 라인 / 라디오 보더 |
| `--wc-mute` | `#7A6B65` | 보조 텍스트 (캡션, sub) |
| `--wc-mute-2` | `#A0938C` | 더 약한 텍스트 |
| `--wc-soft` | `#F4ECE6` | 호버 배경 / soft fill |

### Semantic accents
| Token | Value | 역할 |
|---|---|---|
| `--wc-warn` | `#C8842A` | 경고 / 노란 박스 보더 |
| `--wc-go` | `#2F7D5B` | 성공 / 수익 양수 |
| `--wc-blue` | `#2D5BD7` | 정보 / 리더보드 등 액션 |
| `--wc-down` | `#C03A3A` | 손실 / 음수 |
| `--wc-gold` | `#FFD96B` | Prize ribbon, eyebrow dot |
| `--wc-gold-deep` | `#B8941A` | Ribbon 그림자 |

### Club colors (group identity, `--gp` per-instance)
- Gunner red `#E11D2E` / Kop red `#C8102E` / Blue `#1E3A8A`
- 카드별로 인라인 `style={{ "--gp": "..." }}`로 동적 주입 → 보더, 모토 strip, 통계 b 색에 사용

---

## 3. Typography

- **Font family**: 글로벌 Pretendard / SUIT (이미 `app/layout.tsx`의 `next/font/local`로 로드됨, 그대로 사용)
- **Letter-spacing**: 헤딩은 `-0.01em` ~ `-0.03em` (타이트), eyebrow는 `0.12em`~`0.20em` (와이드)
- **Numerals**: 통계/카운트는 `font-variant-numeric: tabular-nums` 항상

| 역할 | Size | Weight | 비고 |
|---|---|---|---|
| Hero h1 | `clamp(40px, 6vw, 56px)` | 900 | line-height 1.05, `-0.03em` |
| Section h2 | `26px` | 800 | `-0.02em` |
| Card name (group) | `28px` | 900 | `-0.02em`, line-height 1 |
| Card heading (action) | `18px` | 800 | `-0.01em` |
| Step heading | `16px` | 700 | line-height 1.3 |
| Body | `13–14px` | 400 | line-height 1.5–1.65 |
| Caption / sub | `12px` | 500 | `--wc-mute` |
| Eyebrow | `11px` | 700–800 | uppercase, `0.12–0.20em` letter-spacing |
| Step number | `28px` | 900 | tabular, burgundy |
| Stat number | `22–28px` | 900 | tabular, `--gp` 또는 burgundy |
| Countdown number | `32px` | 800 | tabular |

---

## 4. Spacing scale

기본 단위: **4 / 6 / 8 / 12 / 16 / 20 / 24 / 28 / 32 / 40 / 48** (px)
- 섹션 좌우 padding: `px-4` (모바일) → 컨테이너 `mx-auto max-w-{3xl|4xl|5xl}`
- 섹션 vertical: `py-8 ~ py-16`
- 카드 padding: `20–24px`
- Hero padding: `px-6 py-12 sm:px-12 sm:py-16 lg:px-16 lg:py-20`

## 5. Border radius

| 위치 | 값 |
|---|---|
| Hero | `24px` (rounded-3xl) |
| Card | `12px` (대), `10px` (중), `8px` (소) |
| Pill / chip / tab | `999px` |
| 작은 button | `6px` |
| 중간 button | `8px` |
| Action card / register card | `12–14px` |
| Podium card | `16px` |

## 6. Shadows

세 단계 + 1px 라인 합성 — **그림자가 항상 라인을 동반**:

```css
--wc-shadow-1: 0 1px 2px rgba(26,20,22,0.04), 0 0 0 1px var(--wc-line);
--wc-shadow-2: 0 4px 16px rgba(26,20,22,0.06), 0 0 0 1px var(--wc-line);
--wc-shadow-3: 0 12px 40px rgba(126,26,47,0.12), 0 0 0 1px var(--wc-line);
```

호버 시 1단계 → 2단계 → 3단계로 상승 + `translateY(-2px ~ -4px)`.

## 7. Borders

- 기본 카드: `border: 1px solid var(--wc-line)` (대신 shadow의 inset 1px 활용 시 생략 가능)
- 강조 카드: `border: 2px solid var(--gp)` 또는 `border-top: 4px solid var(--gp)`
- 라디오 / 체크박스: `border: 2px solid var(--wc-line-2)` → on 시 `--gp`
- 노란 경고박스: `border: 2px solid var(--wc-warn)`

---

## 8. Layout patterns

- 컨테이너 폭: `max-w-3xl` (텍스트 위주), `max-w-4xl` (베팅), `max-w-5xl` (랜딩 / 리더보드)
- 페이지 좌우 padding: `px-4` (sm 이하), `pt-6 sm:pt-10`, `pb-16`
- 그리드 패턴: 데스크탑 3-column → 모바일 1-column (760px breakpoint)
- "eyebrow + h2 + sub" 섹션 헤더 구조가 모든 섹션에서 반복

---

## 9. Component patterns (재사용 가능)

### Section header
```html
<header class="wc-sec-head">
  <div class="wc-sec-eb">SECTION KEY</div>
  <h2 class="wc-sec-h2">한국어 제목</h2>
  <p class="wc-sec-sub">설명</p>
</header>
```

### Eyebrow + headline (페이지 헤더)
```html
<div class="wc-sec-eb">PAGE KEY</div>
<h1 style="font-size: clamp(28px, 4.5vw, 36px); font-weight: 900; letter-spacing: -0.02em; color: var(--wc-ink); line-height: 1.15">제목</h1>
```

### Hub action card (선택지 카드)
- `wc-action-card` + 인라인 `--gp` 주입 → 위쪽 4px 컬러 보더, 호버 시 상승

### Group/path card (정체성 카드)
- `wc-path` + `--gp` → 보더 + 우상단 큰 컬러 원 + 모토 인용 strip

### Fact card (강조 알림, 노랑)
- `wc-fact-card` — 노란 그라디언트 + warn 보더 + 원형 아이콘

### Hero
- `wc-hero` (burgundy 그라디언트) + `wc-hero-grid` (격자 마스크) + `wc-orb-r/-w/-b` (블러된 색 orb 3개) + `wc-prize-ribbon` (회전된 황금 리본)

### Buttons
- Primary big: `wc-btn-primary-big` (burgundy bg, 14px 32px padding, font 700, radius 8px)
- Hero CTA: `wc-btn-on-hero` (white bg → gold hover)
- Reg CTA: `wc-reg-cta` (full width, padding 16/28)
- Soft pill: leaderboard tabs `wc-lb-tabs button` (radius 999px, off=흰색 보더 / on=burgundy fill)

### Tabs (pill style)
- 컨테이너 `flex gap-6px flex-wrap`
- 각 탭: `padding: 6px 14px; border: 1px solid var(--wc-line-2); border-radius: 999px; font-size: 12px; font-weight: 600`
- on 상태: `background: var(--wc-burgundy); color: white; border-color: var(--wc-burgundy)`

### Podium card (1위 카드)
- `wc-rp-card` — 컬러 보더 2px + 위에서 내려오는 컬러 글로우 + 큰 트로피 이모지 + 인라인 stats 그리드

### Leaderboard "me" strip
- `wc-lb-me` — 가로 그라디언트 (gp → 25% 검정 mix) + 흰 글씨 + 우측 4-grid 통계

---

## 10. Transitions / animations

- 일반: `transition: transform 0.2s, box-shadow 0.2s` (카드 호버)
- 버튼: `transition: filter 0.15s, transform 0.1s, background 0.15s` — `:hover { filter: brightness(1.08) }` + `:active { transform: translateY(1px) }`
- Pulse keyframe: `wc-pulse` (이벤트 신호용, 빨간 점 외곽 ring)
- 모든 transition은 `prefers-reduced-motion: reduce` 대응을 globals.css의 universal selector로 자동 처리됨 (이미 적용)

---

## 11. Iconography

- **lucide-react** (이미 의존성에 있음): Trophy, Users, BarChart3, Gamepad2, Lock, ArrowRight 등
- 아이콘 크기: `h-5 w-5` (16~20px) 본문, `h-6 w-6` 카드, `h-3.5 w-3.5` 인라인
- 아이콘 색: 인라인 `style={{ color: "var(--wc-burgundy)" }}` 또는 `var(--gp)` (그룹 컨텍스트)
- 이모지: 🏆 (트로피), 👑 (king), ⚠ (경고) — 이미 시안에서 사용 중

---

## 12. 적용 전략 — **사용자 승인 필요**

월드컵 토큰은 `.worldcup-scope` 안에만 살아있음. 이걸 다른 페이지에 옮기는 방법 3가지:

| 옵션 | 장점 | 단점 |
|---|---|---|
| **A. 같은 `.worldcup-scope` wrapper 재사용** (가장 빠름) | 즉시 작동, 0 리팩터 | 클래스 이름이 "worldcup" — 의미상 어색 |
| **B. 새 scope 클래스로 리네임** (예: `.editorial-scope`) | 의미 정합 | wc-tokens.css의 모든 셀렉터 변경 필요 |
| **C. 글로벌로 승격** | 어느 페이지서나 사용 | shadcn 토큰과 충돌 위험 ↑ |

**Phase 1 시작 전에 사용자 결정 필요**.

---

## 13. 즉시 멈춤 조건 (이 디자인을 다른 페이지에 적용할 때 미리 알린)

다음 케이스는 Phase 2에서 멈추고 묻는다:

1. **동적 N개 정보** vs 시안의 정적 3개 — 예: `community/[slug]` 가 카테고리 카드 N개일 때 `.wc-paths` 그리드가 깨짐
2. **인터랙션이 시안에 없는 종류** — 예: prediction 페이지의 "슬립 사이드 패널" 같은 sticky 우측 패널은 시안 prediction.html에 있지만 wc-tokens.css에는 슬립 스타일이 빠짐 (`.slip` 등은 styles.css 시안에만 있음)
3. **shadcn 컴포넌트 (Card, Dialog, Button) 와 충돌** — 대상 페이지가 shadcn `<Card>` 사용 시, wc 토큰과 shadcn 토큰이 같이 카드를 렌더하면 색상이 어긋남
4. **다국어 / RTL** — 시안은 한국어/영문 mix, 다른 언어 시 letter-spacing 깨짐
5. **다크모드** — 시안은 라이트 only, 대상 페이지가 다크모드 지원이면 토큰 매핑 필요

---

## 14. 시안에는 있고 wc-tokens.css 에는 없는 것 (필요 시 추가)

설계상 worldcup이 hub라서 누락된 것들 — 다른 페이지에 옮길 때 wc-tokens.css에 추가 작성 필요할 수 있음:

- **slip / 베팅 사이드 패널** (`.slip`, `.slip-list`, `.odd`, `.mrow`) — `prediction` 페이지로 옮길 때 필수
- **Header/sidebar/footer chrome** (`.hdr`, `.side`, `.ftr`) — 이미 root layout이 처리 중이므로 무시 가능, 하지만 hdr 강조 색은 동기화 필요
- **My profile page 통계 줄** (`.my-head`, `.my-tr`, `.tag-win/.tag-lose`) — `my-predictions`, `my-posts` 옮길 때
- **FAQ row** (`.faq-row`, `.faq-mk`)
- **모달** (`.modal`, `.modal-bg`)
- **Share card** (`.sc-formal`, `.sc-meme`, `.sc-data`)
- **Schedule pill** (`.pb-pill.go/.wait/.done`)

이건 Phase 2 진행 중 필요한 것만 점진적으로 wc-tokens.css에 옮겨 추가.
