# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage
/test-results
/playwright-report

# next.js
/.next/
/.next-avatar-lab/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db

# Windows special device files
nul
nul.*
*.nul

# Temporary files
_tmp_*
localhost_*.json
.playwright-mcp/

# Test/dev data files (root level)
/2026_*.json
/260*.json
/future_games.json
/bet365_*.json
/current.png
/image.png

# Screenshots & temp images (root level)
/*.png
/*.webp
/*.jpg
/*.jpeg

# Environment files (non-local)
.env.production
.env.vercel-check

# MCP config
.mcp.json

# Supabase data dumps
gongnori.fan-*.json

# Debug/inspect files
standings-*.json
standings-*.html

# Misc data dumps
/10.5.0.2_*.json
/console-debug.txt
*.sh.tmp

# 서브 패키지 node_modules — data/ 하위는 각자 package.json 을 갖는 별도 워크스페이스라
# 루트 /node_modules 규칙에 안 걸린다. 글로브로 한 번에 막는다
# (data/agent-test 는 이 규칙이 없어 2,410 파일이 추적되고 있었다)
data/*/node_modules/
data/crawlers/logs/
.gstack/

# Claude Code 런타임 락 파일
.claude/scheduled_tasks.lock

# Claude Code 세션 내부 추적 파일
.claude-pixellab-jobs.json

# Supabase CLI 임시 파일 (auto-generated)
supabase/.temp/

# PixelLab ZIP 추출 잔재 (이전 세션에서 root 에 풀린 임시물 — 실제 에셋은 public/metaverse/avatars/)
/avatar-pro/
/avatar-pro-xl/

# 구매한 Colin/Chloe 치비 원본 세트 (CGTrader, 158MB — GLB 빌드 소스, repo 에 안 들어감)
/avatar/

# 아바타/QA 렌더 산출물
/output/

# Playwright audit 결과 (스크린샷, trace.zip 등)
tests/audit/reports/
tests/audit/last-run.log

# 디자인 시안 (작업용, repo 에 안 들어감)
design_handoff_minimal_sport/
design-references/

# CWV 실행 로그 (generated artifact)
tests/audit/last-cwv.log

# 디자인 스왑 작업용 베이스라인 (일회성 산출물)
.refactor-baseline/

# E2E 봇 자격증명 (Clerk Backend API로 생성된 테스트 계정)
tests/e2e/fixtures/bots.json
tests/e2e/reports/
# E2E 환경 오버라이드 (로컬 Supabase 키 — 머신별)
tests/e2e/.env.e2e
aseprite-mcp/

# 애그리게이터 학습 하니스 산출물 (교정본은 config/agg-corrections.json 로 커밋)
workspace/agg-training/round-*.md
workspace/agg-training/fixture.json

# 로컬 권한 승인 기록 — 토큰 등 비밀이 섞일 수 있어 커밋 금지 (2026-07-26 푸시 보호 적발)
.claude/settings.local.json
.claude/settings.local.json.bak

# 뉴스 카드 원본 아트 — 조판만 바꿀 때 재생성을 피하려고 남기지만, 장당 1.3MB라 커밋하지 않는다
public/images/_news-team-raw/

# live-football-api 프로브 캐시 (크레딧 절약용 로컬 응답 캐시)
.lfa-probe-cache/

# 팀명 CSV 왕복 산출물 (운영자 작성분 — 커밋 대상 아님)
missing-teams.csv
missing-players.csv
public/images/_polish-raw/

# 스크래치/드래프트 스크립트 (밑줄 접두사 = 임시, 커밋 대상 아님)
scripts/_*

# 브레인스토밍/조사 세션 산출물 (날짜별 디베이트·리뷰 마크다운, CSV)
workspace/

# 임시 probe/캡처 산출물
.playwright-cli/
arsenal-results-raw.json
match-ft.html
match-upcoming.html
