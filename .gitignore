# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage
/test-results
/playwright-report

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db

# Windows special device files
nul
nul.*
*.nul

# Temporary files
_tmp_*
localhost_*.json
.playwright-mcp/

# Test/dev data files (root level)
/2026_*.json
/260*.json
/future_games.json
/bet365_*.json
/current.png
/image.png

# Screenshots & temp images (root level)
/*.png
/*.webp
/*.jpg
/*.jpeg

# Environment files (non-local)
.env.production
.env.vercel-check

# MCP config
.mcp.json

# Supabase data dumps
gongnori.fan-*.json

# Debug/inspect files
standings-*.json
standings-*.html

# Misc data dumps
/10.5.0.2_*.json
/console-debug.txt
*.sh.tmp

# Crawler node_modules
data/crawlers/node_modules/
data/crawlers/logs/
.gstack/

# Claude Code 런타임 락 파일
.claude/scheduled_tasks.lock

# Claude Code 세션 내부 추적 파일
.claude-pixellab-jobs.json

# Supabase CLI 임시 파일 (auto-generated)
supabase/.temp/

# PixelLab ZIP 추출 잔재 (이전 세션에서 root 에 풀린 임시물 — 실제 에셋은 public/metaverse/avatars/)
/avatar-pro/
/avatar-pro-xl/

# Playwright audit 결과 (스크린샷, trace.zip 등)
tests/audit/reports/
tests/audit/last-run.log

# 디자인 시안 (작업용, repo 에 안 들어감)
design_handoff_minimal_sport/
design-references/

# CWV 실행 로그 (generated artifact)
tests/audit/last-cwv.log

# 디자인 스왑 작업용 베이스라인 (일회성 산출물)
.refactor-baseline/

# E2E 봇 자격증명 (Clerk Backend API로 생성된 테스트 계정)
tests/e2e/fixtures/bots.json
tests/e2e/reports/
# E2E 환경 오버라이드 (로컬 Supabase 키 — 머신별)
tests/e2e/.env.e2e
aseprite-mcp/

# 애그리게이터 학습 하니스 산출물 (교정본은 config/agg-corrections.json 로 커밋)
workspace/agg-training/round-*.md
workspace/agg-training/fixture.json

# 로컬 권한 승인 기록 — 토큰 등 비밀이 섞일 수 있어 커밋 금지 (2026-07-26 푸시 보호 적발)
.claude/settings.local.json
