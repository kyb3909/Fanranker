/**
 * 사가 런타임 설정 — Phase A (여름 이적시장).
 *
 * 윈도우 전환(겨울 이적시장 등)은 이 값 하나로 갈린다 — identity_key 에 박히므로
 * 윈도우가 바뀌면 같은 선수라도 새 사가가 열린다 (설계 의도: 시장마다 새 드라마).
 */
export const SAGA_WINDOW_KEY = "2026-summer"

/** 대량 정산 기준 시각 — 영국 마감(8/31) 이후 여유, 발표는 아침 (오너 확정 2026-08-03) */
export const SAGA_DEADLINE_KST = "2026-09-01T09:00:00+09:00"
