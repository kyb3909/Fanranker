# gstack 사용 가이드

> **설치 위치**: `~/.claude/skills/gstack/`
> **설치일**: 2026-03-27
> **버전**: 0.12.5.0
> **요구사항**: Bun 1.3.11 (설치 완료)

---

## 설치 상태

- [x] Bun 런타임 설치 (`~/.bun/bin/bun`)
- [x] gstack 클론 및 빌드
- [x] Claude Code 스킬 29개 등록
- [x] 브라우저 바이너리 빌드 (`browse/dist/browse.exe`)

---

## 사용 가능한 명령어 (슬래시 커맨드)

Claude Code 새 세션에서 `/` 입력 후 사용할 수 있습니다.

### 핵심 명령어 (자주 쓸 것)

| 명령어 | 역할 | 설명 |
|---|---|---|
| `/review` | 코드 리뷰 | 시니어 엔지니어 관점에서 현재 변경사항 리뷰 |
| `/qa` | QA 테스트 | 실제 브라우저로 사이트 접속해서 UI/기능 테스트 |
| `/qa-only` | QA만 실행 | 리뷰 없이 브라우저 테스트만 |
| `/browse` | 브라우저 제어 | Chromium 브라우저 직접 조작 (로그인 유지) |
| `/plan-eng-review` | 엔지니어링 리뷰 | 기술 설계/아키텍처 관점 검토 |

### 기획/디자인 리뷰

| 명령어 | 역할 | 설명 |
|---|---|---|
| `/office-hours` | 제품 전략 | 코딩 전 "이게 맞는 방향인지" 점검 |
| `/plan-ceo-review` | CEO 관점 | 비즈니스/시장 관점에서 기획 검토 |
| `/plan-design-review` | 디자인 리뷰 | UI/UX 관점에서 설계 검토 |
| `/design-consultation` | 디자인 상담 | 디자인 의사결정 도움 |
| `/design-review` | 디자인 전체 리뷰 | 구현된 UI 전체 검토 |
| `/cso` | 보안 리뷰 | 보안 관점 검토 |

### 배포/릴리즈

| 명령어 | 역할 | 설명 |
|---|---|---|
| `/ship` | 배포 | PR 생성 + 코드 리뷰 + 머지 자동화 |
| `/land-and-deploy` | 릴리즈 | 브랜치 머지 + 배포 + 검증 |
| `/setup-deploy` | 배포 설정 | CI/CD 설정 도우미 |
| `/canary` | 카나리 배포 | 점진적 배포 관리 |

### 자동화/도구

| 명령어 | 역할 | 설명 |
|---|---|---|
| `/autoplan` | 자동 계획 | 작업 분석 → 자동 실행 계획 생성 |
| `/investigate` | 디버깅 | 버그/이슈 원인 추적 |
| `/codex` | 멀티 모델 | 다른 AI 모델에 세컨드 오피니언 요청 |
| `/retro` | 회고 | 완료된 작업에 대한 회고 분석 |
| `/benchmark` | 벤치마크 | 성능 측정 |
| `/document-release` | 릴리즈 문서 | 변경사항 문서화 |

### 안전 장치

| 명령어 | 역할 | 설명 |
|---|---|---|
| `/careful` | 신중 모드 | 위험한 변경 전 한번 더 확인 |
| `/freeze` | 동결 | 특정 파일/영역 변경 금지 |
| `/unfreeze` | 동결 해제 | freeze 해제 |
| `/guard` | 가드 | 특정 규칙 위반 방지 |

### 브라우저/설정

| 명령어 | 역할 | 설명 |
|---|---|---|
| `/setup-browser-cookies` | 쿠키 설정 | 브라우저 로그인 세션 가져오기 |
| `/connect-chrome` | Chrome 연결 | 실행 중인 Chrome에 연결 |
| `/gstack-upgrade` | 업그레이드 | gstack 최신 버전으로 업데이트 |

---

## 사용 예시

### 1. 코드 리뷰 받기

```
/review
```
현재 git diff 기준으로 변경된 코드를 시니어 엔지니어 관점에서 리뷰합니다.
버그, 성능, 보안, 코드 품질 등을 체크합니다.

### 2. QA 테스트 실행

```
/qa
```
변경사항을 분석한 뒤 실제 브라우저(Chromium)로 사이트에 접속해서 테스트합니다.
클릭, 입력, 스크린샷 등 실제 사용자 행동을 시뮬레이션합니다.

### 3. 기획 검토 받기

```
/office-hours
```
"이 기능을 만들어야 하는 이유가 맞나?" 레벨에서 제품 전략을 점검합니다.
코딩 시작 전에 방향성 확인용으로 유용합니다.

### 4. 브라우저로 사이트 확인

```
/browse
```
Chromium을 열어서 직접 gongnori.fan에 접속하고 조작할 수 있습니다.
로그인 세션이 유지되므로 인증이 필요한 페이지도 테스트 가능합니다.

### 5. 보안 점검

```
/cso
```
현재 코드의 보안 취약점을 분석합니다.

### 6. 디자인 리뷰

```
/plan-design-review
```
현재 UI/UX를 디자이너 관점에서 검토합니다.

---

## 주의사항

1. **새 Claude Code 세션에서 사용** — 현재 세션이 아닌 새 세션에서 `/` 명령어로 접근
2. **Windows 호환성** — macOS 기준으로 만들어져서 일부 기능(쿠키 가져오기 등) 제한 가능
3. **브라우저 기능** — `/browse`, `/qa`는 Chromium이 실행되므로 GUI 환경 필요
4. **토큰 사용량** — 일부 명령어(ship, plan-ceo-review)는 20,000+ 토큰 소모
5. **SuperClaude와 공존** — 기존 SuperClaude 프레임워크와 함께 사용 가능 (충돌 없음)

---

## 업그레이드 방법

```
/gstack-upgrade
```
또는 수동:
```bash
cd ~/.claude/skills/gstack && git pull && bash setup
```
