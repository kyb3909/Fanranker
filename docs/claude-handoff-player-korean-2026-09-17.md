# 클로드 인계 — 모든 공개 선수명 한글 표시

아래 내용을 Claude Code 작업 지시로 사용한다. 이전의 “최종 검증만 하면 완료” 인계를 대체한다.

```text
gongnori.fan에서 선수 이름이 영어로 나오는 문제를 이어서 해결해줘.

작업 경로: D:\Projects\새 폴더\adding(test)\community

사용자 요구: 공개 화면의 선수 이름은 무조건 실제 선수의 한글 이름으로 나와야 한다.
영문 약어를 영문 풀네임으로 바꾸는 것은 해결이 아니다.
이름을 숨기거나 “선수”, “확인 중” 같은 문구로 바꾸고 완료 처리하지 마.

먼저 읽기:
- CLAUDE.md
- docs/qa-2026-09-17-player-names.md (상단의 사용자 요구 정정이 최우선)
- docs/qa-2026-09-17-remediation-plan.md의 3번

이전 작업의 실제 상태:
- 2026-09-17 05:05 KST 기록상 운영 배포: dpl_53spf5TUwL48DHndECaz7Bt3BZ5J.
- 저장 라인업 329건과 MoTM 108개, 총 437행 수정.
- 표시 필드 2,717곳 중 한글 전환은 202곳이고 2,515곳은 여전히 영어다.
- 당시 적용 검사에서 기존 투표 29건과 식별자 보존을 확인했다.
- 선수 사전 5,175개 전량 조회 검증은 news_alias_dictionary의 관리자 후보 조회에 관한 것이다.
  경기 라인업은 별도 team_squads를 읽으므로, 이것만으로 경기 화면 한글화를 입증할 수 없다.
- plan-v2.json의 unresolved는 식별 모호 3,870곳과 번역 충돌 2곳이다.
  반복 노출을 포함한 과거 필드 수이며, unmatched를 제외해 영문 잔여 전체 수가 아니다.
- 위 숫자는 과거 기록이다. 현재 운영 상태는 새로 확인해라.

작업 순서:
1. git status와 배포 manifest를 확인하고 다른 작업의 미커밋 변경을 보존해라.
2. 기존 Supabase에서 기사 선수 사전, team_squads, 저장 라인업, MoTM을 전체 페이지 조회해라.
   같은 인물임이 검증된 한글명을 대조하고, 현재 영문·혼합 표기의 선수 수와 표시 필드 수를 구분해라.
3. 다음 경로를 우선 조사하고 수정해라.
   - lib/lfa/player-name.ts: 한글명 미확보·식별 실패 시 영문 원문/풀네임 반환
   - lib/dictionary/relocalize-labels.ts: full-name을 해결로 취급하고 혼합 한글/영문을 건너뛰는 판정
   - lib/dictionary/squad-names.ts: 기사 사전과 별개인 팀 명단 조회
   - lib/news/notation/player-naming-queue.ts: 기사 사전에 한글명이 있다는 이유로
     실제 경기 화면에는 영어로 나오는 선수를 누락 목록에서 제외할 수 있는 판정
   - 실제 공개 화면과 API의 다른 선수명 반환 경로도 찾아 같은 기준을 적용
4. 기존 확정 한글명을 먼저 재사용해라. 등록이 없으면 신뢰할 수 있는 출처에서
   인물과 한글 표기를 확인하고 기존 검수 경로로 보완해라.
   번역 충돌과 동명이인은 근거로 해결하고, 이름 유사도만으로 다른 선수를 합치지 마.
   원문·공급자 ID는 내부 식별에 보존하되 공개 선수명의 대체값으로 사용하지 마.
5. 앞으로의 수집·조회뿐 아니라 이미 저장된 영문 표기도 바로잡아라.
   변경 전후 미리보기, 동시 수정 충돌 검사와 복구 자료를 준비해라.
   선수 ID·등번호·포메이션·이벤트·투표 key·기존 투표는 보존해라.
6. 기존 적용 무결성 검사는 다음 명령으로 이어가되, 이것을 한글화 완료 검사로 착각하지 마.
   pnpm.cmd exec tsx output/player-names-rollout-20260917/verify-data.ts
   pnpm.cmd exec tsx scripts/relocalize-player-names.ts --out=output/player-names-rollout-20260917/post-apply-plan.json
   출력 파일이 이미 존재하면 기존 증거를 덮어쓰지 말고 새 경로를 사용해라.
   verify-data.ts의 votes는 과거 검증 파일을 읽으므로 현재 DB 재검증과 구분해라.
   기존 apply 명령을 그대로 재실행하지 마.
7. 관련 회귀 테스트와 타입 검사를 하고 API·캐시·실제 운영 화면에서 한글 표시를 검증해라.
   단순 재적용 후보 0건, 한글 문자 하나 포함, 영어 이름 숨김으로 완료 처리하지 마.
   운영 반영 여부와 검증 범위를 명시하고, 영문 잔여나 표기 미확정이 있으면 미완료로 보고해라.
8. 실제 결과와 증거 파일을 위 인계 문서 및 해결안에 갱신해라.

제약:
- 기존 Supabase ekysrlhdrapmsnrkytif만 사용. Docker·별도 DB 준비로 범위를 넓히지 마.
- 저장 데이터로 조사하며 유료 LFA 라인업을 재구매하지 마.
- 대상 외 변경의 일괄 커밋·배포 금지. git push는 사용자가 직접 한다.
- 비밀키와 환경변수 값을 출력하지 마.
- 가입·탈퇴 등 다음 QA 항목으로 넘어가지 말고 선수 한글 표시 문제를 끝내라.

최종 보고에는 한글화된 선수 수와 반복 표시 필드 수, 남은 영문·혼합 표기,
표기 충돌, 오매칭 여부, 데이터 보존, 실제 화면 확인 결과를 구분해라.
```
