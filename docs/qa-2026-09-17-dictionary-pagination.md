# 관리자 선수 사전 전체 조회 — 2026-09-17

상태: **2026-09-17 04:26 KST 운영 반영 완료.** 남은 항목 중 2번을 완료했다.

## 변경

- 후보 GET API의 선수 사전 단일 조회를 공통 `fetchDictionaryRows`로 교체했다. 기존 선수 범위와 뉴스 7일/500건·사가 200건의 후보 수집 정책은 유지한다.
- 사전 또는 뉴스·사가 조회가 실패하면 부분 후보나 빈 후보를 반환하지 않고 `503`, `Cache-Control: no-store`로 재시도를 안내한다.
- 공통 함수는 정확히 20,000행인 경우 1행 추가 조회로 끝을 확인한다. 20,001행 이상은 안전 상한 오류로 처리한다. 오류나 데이터 없는 응답을 정상 빈 사전으로 처리하지 않는다.
- 화면은 조회 대기·실패·정상 빈 결과를 구분한다. 갱신 실패 시 이전 후보를 보존하고 재시도 버튼을 제공하며, 실패가 해소되기 전 등재 동작은 막는다.
- 기존 사전 등재·별칭·대표 표기 변경 API의 쓰기 동작은 수정하지 않았다.

## 검증

- Vitest 6파일 57개 통과: 전체 조회 10개, 후보 API 11개, 실제 SWR을 사용한 화면 상태 3개, 사전 재검사 6개, 기존 관리 API 25개, 접근 경로 규칙 2개.
- 1,001번째 이후의 대표명·별칭 제외와 유사 항목 제안, 중간 페이지 실패, 정확한 1,000/20,000행 경계, 상한 초과, 권한 검사, 화면 재시도·기존 후보 보존을 검증했다.
- 작업 폴더 TypeScript 검사와 변경 코드·테스트 ESLint 통과.

## 기존 Supabase 읽기 전용 확인

- 대상: `ekysrlhdrapmsnrkytif`. 사전·기사·계정 데이터 쓰기 없음.
- 04:13 KST 확인: 실제 선수 사전 5,175행. 기존 단일 조회 1,000행, 새 공통 조회 5,175행으로 정확한 DB 건수와 일치했다.
- 기존 조회에 빠지던 4,175행에는 아론 완비사카·압데사마드 에잘줄리 등이 포함된다.
- 당시 뉴스 63행·사가 2행의 미등재 후보는 수정 전후 모두 21개였다. 실제 후보가 줄었다고 주장하지 않는다. 전체 사전 조회를 통해 판정·유사 표기 제안에 필요한 범위를 복구한 것이다.
- 실제 REST의 사전 끝 경계 조회도 빈 배열을 반환하는지 확인했다.
- 공통 함수가 뉴스 파이프라인에서 읽는 전체 6개 분류도 5,389행으로 DB의 정확한 건수와 일치했다. 운영 데이터는 20,000행 안전 상한 이하다.
- 증거: `output/dictionary-rollout-20260917/database-readonly.json`.

## 운영 반영

- 기준 운영 배포는 1번 배당 중복 수정이 반영된 `dpl_ECUf4uo2nWRLzUYg6Yrynx4P2sHU`.
- 변경 대상은 서비스 코드 3개와 테스트 3개다. 기존 운영 소스를 바탕으로 이번 변경만 분리했다.
- 대상 외 실행 관련 파일 2,984개가 기준 운영 소스와 동일하다. 분리 배포본에서도 관련 테스트 57개가 통과했다.
- 실제 관리자 로그인 브라우저 검증은 아직 실행하지 않았다. 화면 상태 테스트는 네트워크 응답을 대체한 실제 React/SWR 렌더링이며, 인증된 운영 화면 검증과 구분한다.
- 실제 분리 배포본 TypeScript 검사와 Vercel 프로덕션 컴파일·린트·타입 검사·정적 페이지 200개 생성·빌드 완료.
- 배포 ID: `dpl_558Q1ThQUNxhRm4XHaHbesZ2heGk`.
- 운영: [gongnori.fan](https://gongnori.fan).
- 배포 URL: [community-cu5ou7tay](https://community-cu5ou7tay-young-kims-projects-fc4245dd.vercel.app).
- 04:26 KST: 프로젝트 production, 운영 도메인, cron 모두 새 배포 ID와 일치하고 프로젝트가 활성 상태임을 확인했다.
- 증거: `output/dictionary-rollout-20260917/{manifest,source-comparison,deployment,promoted}.json`, `deploy.log`.
- DB 마이그레이션·데이터 수정·Git push는 하지 않았다. 되돌리기는 기준 배포 `dpl_ECUf4uo2nWRLzUYg6Yrynx4P2sHU`로 전환하면 된다.
