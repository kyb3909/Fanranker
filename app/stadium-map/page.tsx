import { ImageMap } from "@/components/stadium-map/image-map"

export const metadata = {
  title: "UK Stadium Explorer",
  description: "Explore UK football stadiums on a pixel art world map",
}

export default function StadiumMapPage() {
  return <ImageMap />
}
