"use client"

/**
 * 게시판 [팔로우 버튼] 행.
 *
 * 팔로우 기능 비활성 — 기자(journalist) 시스템 도입 후 복원 예정.
 * 화면에는 아무것도 렌더하지 않는다. (API/route/DB 로직은 그대로 유지)
 * 복원 시 git 히스토리에서 팔로우 상태/토글 구현을 되살릴 것.
 */
export function BoardFollow(_props: { communitySlug: string }) {
  return null
}
